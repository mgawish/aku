#set("content") {
  <div class="container bg-white">
      <img src="#(imageUrl)" class="headerImage"/>
      <br/>
      <br/>
      <h2>#(name)</h2>
      <p>#(content)</p>
  </div>
}
#embed("base")
