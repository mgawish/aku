#set("content") {
  <div class="container pt-3">
    <div class="row">
      #for(post in posts) {
        <div class='col-md-4 content' style='border:solid;text-align:center'>#(post.name)</div>
      }
    </div>
  </div>
}
#embed("base")
